# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% Topic.all.each_with_index do |topic_obj, index| %>
<% 20.times do |n| %>
idea<%= n + index * 20 %>:
  id: <%= n + index * 20 %>
  title: idea_in_<%= topic_obj.title %><%= n %>
  body: idea_about_<%= topic_obj.genre.name %>
  topic_id: <%= index %>
<% end %>
<% end %>
