# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% Idea.all.each_with_index do |idea_obj, index| %>
<% 5.times do |n| %>
comment_to_idea<%= n + index * 5 %>:
    id: <%= n + index * 5 %>
    body: <%= idea_obj.title %>に対するコメント<%= n %>
    commentable_type: Idea
    commentable_id: <%= index %>
<% end %>
<% end %>

<% 1500.times do |n| %>
comment_to_comment<%= n %>:
  body: コメント<%= n %>に対する返信
  commentable_type: Comment
  commentable_id: <%= n %>
<% end %>
