<h1>Topics#index</h1>
<p>Find me in app/views/topics/index.html.erb</p>
<p>
  <%= link_to '新規投稿', new_topic_path %>
  <%= eval(topic_tag_toggle('ideal')) %>
  <%= eval(topic_tag_toggle('trouble')) %>
  <%= eval(topic_tag_toggle('other')) %>
  <% @topics.each do |topic| %>
    <%= topic.id %>
    <%= link_to topic.title, topic_path(id: topic.id) %>
    <%= topic.body %>
    <%= topic.genre_id %><br>
  <% end %>
</p>
