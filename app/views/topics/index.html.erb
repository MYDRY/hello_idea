<h1>Topics#index</h1>
<p>Find me in app/views/topics/index.html.erb</p>
<p>
  <% @genres.each do |genre| %>
    <%= genre.id %><br>
    <%= genre.name %><br>
  <% end %>
</p>
<p>
  <%= button_to 'Ideal', action: 'classfy_topic', genre_id: 1 %>
  <%= button_to 'Trouble', action: 'classfy_topic', genre_id: 2 %>
  <%= button_to 'Other', action: 'classfy_topic', genre_id: 3 %>
  <% @topics.each do |topic| %>
    <%= topic.id %><br>
    <%= topic.title %><br>
    <%= topic.body %><br>
    <%= topic.genre_id %><br>
  <% end %>
</p>
<%= link_to '理想', topics_ideal_path %>
<%= link_to '問題解決', topics_trouble_path %>
<%= link_to 'その他', topics_other_path %>
<%= link_to '新規投稿', new_topic_path %>
