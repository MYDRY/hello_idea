<link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">

<div id="smart_topic_pannel">
  <% if topic.ideas.count == 0 %>
  <div class="idea-count">
    <% else %>
  <div class="idea-count2">
    <% end %>
    <%= topic.ideas.count %><br><p>アイデア</p>
  </div>
  <div class="topic-information">
    <%=link_to topic.title, topic_path(id: topic), class:'topic-title' %>
    <%= link_to topic.body, topic_path(id: topic), class: 'topic-detail' %>
    <div class="user-image">
      <%= link_to image_tag(get_image(topic.user), size: '40', class: 'img-thumbnail'), user_path(id: topic.user) %>
      <%= link_to topic.user.name, user_path(id: topic.user) %>
      <span>
      投稿日時：<%= format_datetime(topic.created_at) %>
      <% if topic.user_id == current_user.id %>
        <%= link_to edit_topic_path(id: topic.id) do %>
          <span class="fa fa-edit"></span>
        <% end %>
        <%= link_to topic_path(id: topic.id), method: :delete do%>
          <span class="fa fa-trash"></span>
        <% end %>
       <% end %>
      </span>
    </div>
  </div>
</div>

<!--<div class="panel panel-success" style="margin: 10px 75px 0px; border-width: 3px;">
  <div class="topic">
    <div class="panel-heading" style="background-color: white">
      <div class="box">
        <ul>
        <li class="b-item1"><%= link_to image_tag(get_image(topic.user), size: '50', class: 'img-thumbnail'), user_path(id: topic.user) %></li>
        <li class="b-item2"><%= link_to topic.user.name, user_path(id: topic.user), style: 'font-size: 20px; color: #007B96; font-weight: 700;' %>
        <span class="fa fa-comment" style="float: right; padding-top: 2%;"><%= topic.ideas.count %></span></li>
        </ul>
      </div>
    </div>
    <div class="panel-body">
      <div class="b-item4"><span title="詳細ページへ"><%=link_to topic.title, topic_path(id: topic) %></span></div>
      <div class="b-item5" style="color: gray; width: 90%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; padding-left: 1vw;">
        <span title="詳細ページへ"><%= link_to topic.body, topic_path(id: topic), style: 'width: 100%; text-align: left;' %></span>
      </div>
    </div>
    <div class="panel-footer" style="text-align: right;">
      投稿日時：<%= format_datetime(topic.created_at) %>
      <% if topic.user_id == current_user.id %>
        <%= link_to edit_topic_path(id: topic.id) do %>
          <span class="fa fa-edit"></span>
        <% end %>
        <%= link_to topic_path(id: topic.id), method: :delete do%>
          <span class="fa fa-trash"></span>
        <% end %>
       <% end %>
    </div>
  </div>
</div>-->