<div class="main">
  <div class="container" style="word-break: break-all;">
    <div style="text-align: center; font-size: 7vh;">
      <%= @word1.word %> ×　<%= @word2.word %>
    </div>
    <br>
    <% @ideas.each do |idea| %>
        <%= render partial: 'seas/simple', locals: { sea: idea.sea }, layout: 'ideas/list_frame' %>
    <% end %>
    <%= link_to "ランダムワードへ戻る", words_path %>
  </div>
</div>
